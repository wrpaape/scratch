#include "smallest_circle/smallest_circle.h"

void
smallest_circle(double *x, double *y, double *r,
                const double *points,
                size_t        length)
{
	*x = 0.0;
	*y = 0.0;
	*r = 0.0;
}
