#include "gtest/gtest.h"  // TEST, EXPECT_*
#include "parse_points.h" // parse_points()

namespace {

static const std::size_t MAX_POINTS = 1000;

} // namespace

TEST(test_parse_points, pass)
{
    SUCCEED();
}
